import './table-of-contents.css'; import TableOfContentsClient from './TableOfContentsClient'; import type { TocItem,TocOptions } from '../lib/toc';
export default function TableOfContents({items,options,className=''}:{items:TocItem[];options:TocOptions;className?:string}){if(!items.length)return null;return <div className={`lexi-toc-wrap ${options.sticky?'is-sticky':''} ${className}`} style={{'--toc-accent':options.accent} as React.CSSProperties}><TableOfContentsClient items={items} title={options.title} defaultOpen={options.defaultOpen} mobileCollapsed={options.mobileCollapsed}/></div>}
