import Header from './Header';import Footer from './Footer';import './starter.css';
export default function Page({post,settings={},skipHeader=false,skipFooter=false}:{post:any;settings?:Record<string,string>;skipHeader?:boolean;skipFooter?:boolean}){return <div className="starter">{!skipHeader&&<Header settings={settings}/>}<main className="starter-page"><article className="starter-container starter-content"><h1>{post.title}</h1>{post.excerpt&&<p className="starter-lead">{post.excerpt}</p>}<div className="starter-prose" dangerouslySetInnerHTML={{__html:post.content||''}}/></article></main>{!skipFooter&&<Footer settings={settings}/>}</div>}
