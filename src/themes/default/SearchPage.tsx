import Header from './Header';import Footer from './Footer';import {Search} from 'lucide-react';import {StarterArchiveHero,StarterArchiveLayout,StarterPaginationNav,StarterPostGrid,type StarterPagination} from './ArchiveComponents';import './starter.css';
export default function SearchPage({query='',posts=[],settings={},pagination,skipHeader=false,skipFooter=false}:{query:string;posts:any[];settings:Record<string,string>;pagination:StarterPagination;skipHeader?:boolean;skipFooter?:boolean}){const summary=query?`${pagination.totalItems} kết quả được tìm thấy`:'Nhập từ khóa để bắt đầu tìm kiếm';return <div className="starter">{!skipHeader&&<Header settings={settings}/>}<StarterArchiveHero eyebrow="Tìm kiếm" title={query?`Kết quả cho “${query}”`:'Tìm nội dung'} summary={summary}/><StarterArchiveLayout><form className="starter-search-form" action="/tim-kiem" method="get"><Search size={19} style={{margin:'auto 0 auto 12px',color:'#98a2b3'}}/><input name="q" defaultValue={query} maxLength={120} placeholder="Nhập từ khóa..." aria-label="Từ khóa tìm kiếm"/><button type="submit">Tìm kiếm</button></form><div style={{marginTop:36}}><StarterPostGrid posts={posts} settings={settings} emptyText={query?'Không tìm thấy nội dung phù hợp.':'Hãy nhập từ khóa để tìm kiếm.'}/></div><StarterPaginationNav pagination={pagination} basePath="/tim-kiem" query={{q:query}}/></StarterArchiveLayout>{!skipFooter&&<Footer settings={settings}/>}</div>}
