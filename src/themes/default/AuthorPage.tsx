import Header from './Header';import Footer from './Footer';import {StarterArchiveLayout,StarterPaginationNav,StarterPostGrid,type StarterPagination} from './ArchiveComponents';import './starter.css';
export default function AuthorPage({author,posts=[],settings={},pagination,skipHeader=false,skipFooter=false}:{author:any;posts:any[];settings:Record<string,string>;pagination:StarterPagination;skipHeader?:boolean;skipFooter?:boolean}){const name=author.name||author.username||'Tác giả',initials=name.split(/\s+/).slice(0,2).map((part:string)=>part[0]).join('').toUpperCase();return <div className="starter">{!skipHeader&&<Header settings={settings}/>}<section className="starter-archive-hero"><div className="starter-container starter-author-card"><div className="starter-avatar">{initials}</div><div><span className="starter-eyebrow">Tác giả</span><h1>{name}</h1><div className="starter-archive-summary">{pagination.totalItems} bài viết đã xuất bản</div></div></div></section><StarterArchiveLayout><StarterPostGrid posts={posts} settings={settings} emptyText="Tác giả chưa có bài viết công khai."/><StarterPaginationNav pagination={pagination} basePath={`/author/${author.username}`}/></StarterArchiveLayout>{!skipFooter&&<Footer settings={settings}/>}</div>}
