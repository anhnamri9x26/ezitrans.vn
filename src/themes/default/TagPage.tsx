import Header from './Header';import Footer from './Footer';import {StarterArchiveHero,StarterArchiveLayout,StarterPaginationNav,StarterPostGrid,type StarterPagination} from './ArchiveComponents';import './starter.css';
export default function TagPage({tag,posts=[],settings={},pagination,skipHeader=false,skipFooter=false}:{tag:any;posts:any[];settings:Record<string,string>;pagination?:StarterPagination;skipHeader?:boolean;skipFooter?:boolean}){return <div className="starter">{!skipHeader&&<Header settings={settings}/>}<StarterArchiveHero eyebrow="Thẻ nội dung" title={`#${tag.name}`} description={tag.description} summary={pagination?`${pagination.totalItems} bài viết`:undefined}/><StarterArchiveLayout><StarterPostGrid posts={posts} settings={settings} emptyText="Chưa có bài viết nào được gắn thẻ này."/><StarterPaginationNav pagination={pagination} basePath={`/tag/${tag.slug}`}/></StarterArchiveLayout>{!skipFooter&&<Footer settings={settings}/>}</div>}
